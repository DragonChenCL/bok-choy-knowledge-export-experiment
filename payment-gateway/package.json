{
  "name": "payment-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "d1:migrate:local": "wrangler d1 migrations apply PAYMENT_DB --local",
    "d1:migrate:remote": "wrangler d1 migrations apply PAYMENT_DB --remote"
  },
  "dependencies": {
    "@waffo/pancake-sdk": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
